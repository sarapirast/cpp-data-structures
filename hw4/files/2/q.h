#ifndef FILES_2_Q
#define FILES_2_Q
#include <iostream>
#include <map>
#include <string>
#include <vector>

// A function that checks if a given vector that represents a complete binary
// tree is a min heap.
bool IsMinHeap(const std::vector<int> &heap);

// A function that converts a given vector into a min heap.
void ConvertToMinHeap(std::vector<int> &in);

#endif /* FILES_2_Q */
